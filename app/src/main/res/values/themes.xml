<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.GoalPulse" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.GoalPulse.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.GoalPulse</item>

        <!-- Pre-Android 12 fallback splash while activity starts -->
        <item name="android:windowBackground">@drawable/splash_screen_legacy</item>
    </style>
</resources>